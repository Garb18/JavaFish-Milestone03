package UserCode.Random;

/**
 * Write a description of interface IRandom here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface IRandom
{
    double returnDouble();
    
    double returnDouble(double min, double max);
}
